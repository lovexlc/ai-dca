# 点击式交互审计修复计划

## 目标
把 2026-05-20 点击式交互审计中的 P0/P1 问题转为可验证的前端修复，优先降低误跳转、子页断层和可访问性歧义。

## 步骤清单
- [done] 1. 定位相关入口与组件：工作台 CTA、交易计划二级 tab、新建建仓流程、持仓收益子页、交易录入面板。
- [done] 2. 修复 P0：工作台 `新建加仓策略` 直接进入 `tradePlans#new`。
- [done] 3. 修复 P0：交易计划 `加仓` tab 只展示/筛选，不再误入创建流程。
- [done] 4. 修复 P0：持仓收益子页保留 `收益明细 / 清仓分析 / 持仓分析 / 交易记录` 子导航。
- [done] 5. 修复 P1：新建建仓计划步骤保护与首屏/参数区减重。
- [done] 6. 修复 P1：新增交易表单改成真正 modal/dialog，并统一提交按钮文案为 `保存交易`。
- [done] 7. 运行 focused lint/build 或可行的静态检查，必要时用浏览器验证关键交互。
- [done] 8. 检查 git diff 并提交。

## 关键决策
- 本轮先按用户给出的优先级推进，不重新做静态结构审计。
- P0 改动必须先落地；P1 采用最小可用的流程保护/语义修复，避免牵涉 P2/P3 大范围重构。
- 不新增 UI 依赖，复用现有 React/Tailwind 结构。
- `加仓` tab 当前代码已映射到 `#home` 并渲染加仓面板，不是 `#new` 创建流；本轮保留该语义并记录为已核对。

## 待确认项
- 浏览器交互验证受当前环境限制未继续强行绕过；本轮以源码路由检查、lint/build 作为替代验证。

## 产出与验证记录
- 2026-05-20 15:05：已开始定位 P0/P1 文件，工作树初始状态干净。
- 2026-05-20 15:10：工作台 `新建加仓策略` 已改为进入 `tradePlans#new`，并保留 `tradePlans` hash，避免进入时被规范化 URL 去掉。
- 2026-05-20 15:11：持仓收益子页已补充同组子导航，可在 `收益明细 / 清仓分析 / 持仓分析 / 交易记录` 间直接切换。
- 2026-05-20 15:12：新建建仓计划已加入步骤保护；第一步移除策略名称并折叠美股快捷标的；第二步不提前展示精确均线价；第三步仅保留总金额、频率、现金留存，价格表默认折叠；第四步确认计划名称。
- 2026-05-20 15:13：新增交易弹层已核对为 dialog 语义、标题关联、Esc 关闭和关闭按钮名称；提交按钮为 `保存交易`。
- 2026-05-20 15:14：`npm run lint` 未通过，失败来自仓库既有 lint 错误（如 data-table hooks、MarketsChartBlock irregular whitespace 等）和大量既有 warning；本轮新增文件未出现 JSX 语法错误。
- 2026-05-20 15:15：`npm run build:app` 默认内存下 OOM；改用 `NODE_OPTIONS=--max-old-space-size=4096 npm run build:app` 后任务长时间无输出并由本轮停止，未得到可用构建结果。
