# 新建建仓计划双栏与金字塔预览修复计划

## 目标
修复步骤生成器在大屏双栏中的空白问题，并将右侧加仓权重预览从拥挤柱状图升级为三列金融信息图。

## 步骤清单
- [done] 1. 定位 `NewPlanExperience.jsx` 的步骤布局、右侧成本预览和权重图结构。
- [done] 2. 让右侧 `策略成本预览` 在步骤 1/2/3/4 全程常驻显示，消除右侧空白。
- [done] 3. 在步骤 4 左侧新增 `确认策略配置` 明细，包含策略名称、标的、模板、频率和档位表格。
- [done] 4. 将权重柱状图替换为三列 `Price / Pyramid / Budget` 金字塔信息图。
- [done] 5. 聚焦 lint / diff / status 验证。
- [done] 6. 提交改动。

## 关键决策
- 只改新建建仓计划体验页，不扩大到其它页面。
- 复用现有 Tailwind / Card / Field / SectionHeading，不新增依赖。
- 右栏保留 sticky 并全步骤常驻；左栏在第 4 步显示确认明细，避免左侧空白。
- 金字塔宽度采用 `35% + 层级递进 40% + 权重系数 15%`，最高限制 94%，保证对称递进且不挤压两侧文本。

## 产出与验证记录
- 已修改 `src/pages/NewPlanExperience.jsx`：右侧策略成本预览不再只在第 4 步显示，而是全步骤常驻。
- 已新增第 4 步左侧 `确认策略配置`：策略名称、投资标的、模板/频率与档位确认表格。
- 已替换右侧权重柱状图为三列金融信息图：左列价格/条件，中列阶梯金字塔，右列预算/资金占比，并加入中轴虚线、渐变、hover 阴影与 shimmer 扫光。
- 聚焦验证：`npx eslint src/pages/NewPlanExperience.jsx` 退出码 0；仅剩 `_error` 既有 unused warning，无 error。
- Git 检查：提交前已查看 `git diff` 与 `git status`，确认仅包含本需求相关源码与本计划文件。
