# Deprecated Android Project

This legacy Capacitor Android project is deprecated.

The old Android GCM/FCM notification path has been removed from the notify Worker and from the web UI. Use the current notification channels instead:

- iOS Bark
- Server酱³
- PC browser WebSocket notifications

Do not add new Android push features here. Any future mobile client work should be done in a separate maintained app project.
