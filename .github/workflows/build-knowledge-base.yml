name: Build AI knowledge base

on:
  push:
    branches: [main]
    paths:
      - 'README.md'
      - 'demand.md'
      - 'AGENTS.MD'
      - 'workers/README.md'
      - 'docs/**/*.md'
      - 'scripts/build_kb.mjs'
      - '.github/workflows/build-knowledge-base.yml'
  schedule:
    # 每天 UTC 18:00 跑一次（= Asia/Shanghai 02:00），从 Notion 同步 KB
    - cron: '0 18 * * *'
  workflow_dispatch:

concurrency:
  group: build-knowledge-base
  cancel-in-progress: false

jobs:
  build:
    name: Embed docs and upsert to Vectorize
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Checkout ai-dca-andriod (Android app docs)
        uses: actions/checkout@v4
        with:
          repository: lovexlc/ai-dca-andriod
          ref: main
          path: external/ai-dca-andriod

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Build knowledge base
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          KB_INDEX_NAME: ai-dca-kb
          EMBED_MODEL: '@cf/baai/bge-m3'
          KB_DIMENSIONS: '1024'
          EXTRA_SOURCES_DIR: external/ai-dca-andriod
          NOTION_API_KEY: ${{ secrets.NOTION_API_KEY }}
          NOTION_KB_ROOT_PAGE_ID: ${{ secrets.NOTION_KB_ROOT_PAGE_ID }}
        run: node scripts/build_kb.mjs

      - name: Print summary
        if: always()
        run: |
          {
            echo '## Knowledge base build'
            echo "- commit: \`${GITHUB_SHA}\`"
            echo "- ref: \`${GITHUB_REF}\`"
            echo "- index: \`ai-dca-kb\`"
            echo "- model: \`@cf/baai/bge-m3\`"
          } >> "$GITHUB_STEP_SUMMARY"
